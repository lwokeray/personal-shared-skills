# Security Policy

請不要在技能、範例、issue 或 pull request 中提交 API key、access token、cookie、密碼、私密金鑰、內部 URL 或個人識別資料。

本次 initial export 已排除 `.pyc`、`.DS_Store` 與本機技能 registry，並會在發布前掃描常見秘密模式；這不等於完整的 secrets detection、供應鏈審查或上游授權審查。

若發現疑似敏感資料，請先私下通知 repository 維護者，不要在公開 issue 直接貼出秘密內容。
