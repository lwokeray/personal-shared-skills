# Security Policy

請不要在技能檔案、issue、pull request 或範例中提交 API key、access token、cookie、密碼、私有金鑰、內部 URL 或個人識別資料。

若發現疑似敏感資料，請不要公開建立 issue；請先由 repository 維護者以私下方式確認並立即撤除相關內容，再視需要旋轉憑證與清理 git history。

本次初始匯出已對來源 markdown 執行關鍵字掃描，但這不是完整的 secrets detection 或法律審查。公開前仍應由 repository 擁有者檢查新增檔案與 commit diff。
