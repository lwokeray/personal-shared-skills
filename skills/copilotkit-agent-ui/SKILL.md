---
name: copilotkit-agent-ui
description: "Build and integrate CopilotKit and AG-UI into a production AI website with controlled generative UI, shared state, tool rendering, human approval, and an interoperable agent backend. Use when creating a CopilotKit application, AG-UI client/backend integration, agentic chat, tool-rendered React UI, shared state, A2UI, or MCP Apps in a website project."
---

# copilotkit-agent-ui

> **公開狀態：恢復版 metadata-only。** 此入口保留原有技能的觸發描述，但原始 atomic skill body 並未出現在可保留的本機備份中。

## 適用時機

Build and integrate CopilotKit and AG-UI into a production AI website with controlled generative UI, shared state, tool rendering, human approval, and an interoperable agent backend. Use when creating a CopilotKit application, AG-UI client/backend integration, agentic chat, tool-rendered React UI, shared state, A2UI, or MCP Apps in a website project.

## 使用界線

請只把本檔案視為技能路由與觸發 metadata，不要假定其中包含未提供的工具命令、API schema、工作流步驟或第三方授權內容。若需要完整實作，應在此目錄補上經驗證的工作流，並在本檔案中直接或透過一層 references 連結到相關文件。

## 來源

此條目由 `personal-pack-01-agent-mcp-integrations/references/skills/copilotkit-agent-ui.md` 整理而來。
