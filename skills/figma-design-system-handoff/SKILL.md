---
name: figma-design-system-handoff
description: "Turn Figma designs and design systems into production UI through Figma MCP, design tokens, Code Connect, and visual validation. Use when a task includes a Figma URL or node, design-to-code implementation, component mapping, design-system rules, or a request to make implementation faithfully match a Figma design."
---

# figma-design-system-handoff

> **公開狀態：恢復版 metadata-only。** 此入口保留原有技能的觸發描述，但原始 atomic skill body 並未出現在可保留的本機備份中。

## 適用時機

Turn Figma designs and design systems into production UI through Figma MCP, design tokens, Code Connect, and visual validation. Use when a task includes a Figma URL or node, design-to-code implementation, component mapping, design-system rules, or a request to make implementation faithfully match a Figma design.

## 使用界線

請只把本檔案視為技能路由與觸發 metadata，不要假定其中包含未提供的工具命令、API schema、工作流步驟或第三方授權內容。若需要完整實作，應在此目錄補上經驗證的工作流，並在本檔案中直接或透過一層 references 連結到相關文件。

## 來源

此條目由 `personal-pack-02-ai-product-ui-design/references/skills/figma-design-system-handoff.md` 整理而來。
