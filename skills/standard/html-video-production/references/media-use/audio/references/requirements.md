# [MANUS OVERRIDE] Requirements

The Manus path requires no upstream provider credential. Use native media generation by default and only the explicitly permitted local fallbacks in `_manus-overrides/media-generation.md`. Do not run upstream authentication, token, or provider setup instructions.
